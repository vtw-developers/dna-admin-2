<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vtw.dna.monitor.log.repository.ApiLogRepository">
  <select id="count">
    SELECT COUNT(*)
    FROM API_LOG A
    <where>
      <if test="filter.result != null">
        AND A.RESULT = #{filter.result}
      </if>
      <if test="filter.fromTime != null">
        AND A.TIMESTAMP &gt;= #{filter.fromTime}
      </if>
      <if test="filter.toTime != null">
        AND A.TIMESTAMP &lt;= #{filter.toTime}
      </if>
    </where>
  </select>

  <select id="findAll">
    SELECT
    *
    FROM API_LOG A
    <where>
      <if test="filter.result != null">
        AND A.RESULT = #{filter.result}
      </if>
      <if test="filter.fromTime != null">
        AND A.TIMESTAMP &gt;= #{filter.fromTime}
      </if>
      <if test="filter.toTime != null">
        AND A.TIMESTAMP &lt;= #{filter.toTime}
      </if>
    </where>
    OFFSET #{pageable.offset} ROWS FETCH NEXT #{pageable.pageSize} ROWS ONLY
  </select>

  <select id="findById">
    SELECT
      *
    FROM API_LOG A
    WHERE A.MESSAGE_ID = #{messageId}
  </select>
</mapper>
