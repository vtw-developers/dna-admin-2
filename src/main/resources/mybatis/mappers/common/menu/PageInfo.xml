<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vtw.dna.common.menu.pageinfo.repository.PageInfoRepository">
    <select id="count">
        SELECT COUNT(*)
        FROM PAGE_INFO
        <where>
            <if test="filter.name != null and filter.name != ''">
                AND NAME LIKE '%' || #{filter.name} || '%'
            </if>
            <if test="filter.path != null and filter.path != ''">
                AND PATH LIKE '%' || #{filter.path} || '%'
            </if>
        </where>
    </select>

    <select id="pagingList">
        SELECT P.ID, P.NAME, P.PATH, P.READ_ROLE_ID, P.WRITE_ROLE_ID, R.NAME AS READ_ROLE_NAME
        FROM PAGE_INFO P
        LEFT OUTER JOIN ROLE R
        ON P.READ_ROLE_ID = R.ID
        <where>
            <if test="filter.name != null and filter.name != ''">
                AND P.NAME LIKE '%' || #{filter.name} || '%'
            </if>
            <if test="filter.path != null and filter.path != ''">
                AND P.PATH LIKE '%' || #{filter.path} || '%'
            </if>
            <if test="filter.readRoleId != null">
                AND P.READ_ROLE_ID = #{filter.readRoleId}
            </if>
        </where>
        <if test="pageable.sort != null and !pageable.sort.isEmpty()">
            ORDER BY
            <foreach collection="pageable.sort" item="order" separator=",">
                <if test="order.property == 'name'">
                    P.NAME
                </if>
                <if test="order.property == 'path'">
                    P.PATH
                </if>
                <if test="order.property == 'ROLE_NAME'">
                    R.NAME
                </if>
                ${order.direction}
            </foreach>
        </if>
        OFFSET #{pageable.offset} ROWS FETCH NEXT #{pageable.pageSize} ROWS ONLY
    </select>

    <select id="findAll">
        SELECT P.ID, P.NAME, P.PATH, P.READ_ROLE_ID, R.NAME AS READ_ROLE_NAME
        FROM PAGE_INFO P
                 LEFT OUTER JOIN ROLE R
                                 ON P.READ_ROLE_ID = R.ID
    </select>

    <select id="findById">
        SELECT ID, NAME, PATH, READ_ROLE_ID
        FROM PAGE_INFO
        WHERE ID = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO PAGE_INFO (NAME, PATH, READ_ROLE_ID)
        VALUES (#{name}, #{path}, #{readRoleId})
    </insert>

    <update id="update">
        UPDATE PAGE_INFO
        SET NAME         = #{name},
            PATH         = #{path},
            READ_ROLE_ID = #{readRoleId}
        WHERE ID = #{id}
    </update>

    <delete id="delete">
        DELETE
        FROM PAGE_INFO
        WHERE ID = #{id}
    </delete>

    <select id="existsByName">
        SELECT EXISTS(SELECT 1
                      FROM PAGE_INFO
                      WHERE NAME = #{name})
    </select>

</mapper>
